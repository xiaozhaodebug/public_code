# NXP S32K144 OpenOCD Configuration

# J-Link interface
adapter driver jlink
transport select swd
adapter speed 2000

# SWD DAP
swd newdap s32k144 cpu -irlen 4 -ircapture 0x1 -irmask 0xf -expected-id 0x2ba01477
dap create s32k144.dap -chain-position s32k144.cpu

# Target
set CHIPNAME s32k144
target create $CHIPNAME.cpu cortex_m -dap $CHIPNAME.dap

# Flash bank - S32K144 has 512KB internal flash at 0x00000000
# Using the kinetis flash driver (S32K is similar to Kinetis)
flash bank $CHIPNAME.flash kinetis 0x00000000 0x00080000 0 4 $CHIPNAME.cpu

# Working area (SRAM_L: 0x1FFF8000 - 0x1FFFFFFF, 32KB)
$CHIPNAME.cpu configure -work-area-phys 0x1FFF8000 -work-area-size 0x8000

reset_config srst_only srst_nogate
